<a href="{{ $url }}" target="_blank" class="social-icon social-icon-{{ $network }} social-icon-{{ $size ?? 'medium' }}" title="{{ $title ?? ucwords($network) }}">
	<span class="fa fa-{{ $network }}"></span>
</a>